# ── Credenciales — NUNCA subir al repositorio ─────────────────
.env

# ── Dependencias (si se agregan en el futuro) ─────────────────
vendor/
node_modules/

# ── Logs del servidor ──────────────────────────────────────────
*.log
/logs/

# ── Archivos del sistema operativo ────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Caché y temporales de editores ────────────────────────────
.idea/
.vscode/
*.swp
*.swo
*.tmp

# ── PHP caché y temporales ────────────────────────────────────
/tmp/
*.cache

# ── Archivos de sesión PHP (si se guardan en disco) ───────────
/sessions/
